name: default
description: Default local development agent profile

backend: bedrock

# Model config supports either:
# 1) backend-specific map (shown here), or
# 2) a flat map for a single fixed backend.
model:
  ollama:
    host: http://127.0.0.1:11434
    model_id: qwen2.5:1.5b-instruct
  bedrock:
    region: us-east-2
    model_id: us.anthropic.claude-haiku-4-5-20251001-v1:0

token_budget: {}

tools:
  read_file: {}
  list_dir: {}

task_templates: {}

system_prompt: >
  You are a concise assistant.
  Reply briefly and follow instructions exactly.
